name: evaluate-proposal

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: write
  pull-requests: write

jobs:
  evaluate:
    runs-on: ubuntu-latest
    concurrency:
      group: autoanything-evaluation
      cancel-in-progress: false
    env:
      PRIVATE_EVALUATOR_REPO: ${{ vars.AUTOANYTHING_EVALUATOR_REPO }}
      PRIVATE_EVALUATOR_TOKEN: ${{ secrets.EVALUATOR_TOKEN }}
    steps:
      - name: Check out proposal
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install public dependencies
        run: python -m pip install -e .

      - name: Run public validation
        run: python -m unittest discover -s tests -v

      - name: Fetch private evaluator
        if: ${{ env.PRIVATE_EVALUATOR_REPO != '' && env.PRIVATE_EVALUATOR_TOKEN != '' }}
        uses: actions/checkout@v4
        with:
          repository: ${{ env.PRIVATE_EVALUATOR_REPO }}
          token: ${{ env.PRIVATE_EVALUATOR_TOKEN }}
          path: _evaluator

      - name: Run private evaluation
        if: ${{ env.PRIVATE_EVALUATOR_REPO != '' && env.PRIVATE_EVALUATOR_TOKEN != '' }}
        env:
          AUTOANYTHING_BASE_BRANCH: master
          AUTOANYTHING_PR_NUMBER: ${{ github.event.pull_request.number || '' }}
        run: bash _evaluator/score.sh

      - name: Report result and merge or close
        if: ${{ env.PRIVATE_EVALUATOR_REPO != '' && env.PRIVATE_EVALUATOR_TOKEN != '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python _evaluator/reporter.py
