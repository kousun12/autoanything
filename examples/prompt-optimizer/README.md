# Prompt Optimizer Example

This example shows that AutoAnything is not limited to ML training loops. The mutable state is a prompt template, the read-only context is a labeled sample set, and the hidden evaluator could grade the prompt with an LLM-as-judge or exact-match scorer.
