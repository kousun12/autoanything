# Agent Guide

1. Read `problem.yaml` first.
2. Read `leaderboard.md`, `signals.md`, and `history/attempts.json` before choosing an idea.
3. Create a branch named `proposals/<agent>/<idea>`.
4. Edit only the mutable files listed in the problem definition.
5. Commit one coherent idea per branch and push it for evaluation.
